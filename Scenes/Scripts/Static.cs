using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Static : MonoBehaviour
{
    public static int nPagina;
    // Start is called before the first frame update
    void Start()
    {
        nPagina=0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
